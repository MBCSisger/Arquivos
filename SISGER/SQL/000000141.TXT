CREATE TABLE CONFIG_NF 
(
  ID_CFGNF	NUMBER(*) NOT NULL,
  DESCR_NF      VARCHAR2(30),
  MODELO        VARCHAR2(2) NOT NULL,
  SERIE	        NUMBER(*) NOT NULL,
  ATIVO	        VARCHAR2(1),
  CONSTRAINT CONFIG_NF_PK PRIMARY KEY (ID_CFGNF)
)
