CREATE OR REPLACE VIEW CLIENTE2 AS 
SELECT  
   CASE WHEN FJ = 'F' THEN 
        NRCPF
   ELSE 
        NRCGC 
   END AS CPFCNPJ,
 A.*,
(SELECT NOME||'/'||UF FROM CIDADE WHERE CODIGO = A.CODMUNI) AS CIDADE
FROM CLIENTE  A
