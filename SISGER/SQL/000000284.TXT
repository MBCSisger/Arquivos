ALTER TABLE CLIENTE ADD (NRCGC_TMP VARCHAR2(14 BYTE))
$

UPDATE CLIENTE SET NRCGC_TMP = LPAD(TO_CHAR(NRCGC), 14, '0')
$

COMMIT
$

ALTER TABLE CLIENTE DROP COLUMN NRCGC
$

ALTER TABLE CLIENTE RENAME COLUMN NRCGC_TMP TO NRCGC
$

ALTER TABLE CLIENTE MODIFY (NRCGC DEFAULT '0' NOT NULL ENABLE)
$

CREATE INDEX CLIENTE_IDX2 ON CLIENTE (NRCGC)
$

ALTER TABLE EMPRESA ADD IND_OPCAO_IBSCBS VARCHAR2(1) DEFAULT NULL
$

COMMENT ON COLUMN EMPRESA.IND_OPCAO_IBSCBS IS 'Opção IBS/CBS Simples Nacional: N = Recolhimento Unificado (DAS) | S = Recolhimento em Separado (Regime Regular)'
$

UPDATE EMPRESA SET IND_OPCAO_IBSCBS = NULL WHERE CRT <> 1
$

UPDATE EMPRESA 
SET IND_OPCAO_IBSCBS = 'N' 
WHERE CRT = 1 AND IND_OPCAO_IBSCBS IS NULL
$

COMMIT
$
