CREATE TABLE NFERES 
   (	"CHAVE_ACESSO" VARCHAR2(44 BYTE) NOT NULL ENABLE, 
	"NR_NFE" NUMBER(9,0), 
	"DATA_EMISSAO" DATE, 
	"CNPJ_EMITENTE" VARCHAR2(14 BYTE), 
	"NOME_EMITENTE" VARCHAR2(100 BYTE), 
	"VALOR_NF" NUMBER(15,2), 
	"STATUS" NUMBER(1,0) DEFAULT 0, 
	"XML" CLOB, 
	"ULTIMO_EVENTO" VARCHAR2(100 BYTE), 
	"DETALHE_EVENTO" VARCHAR2(500 BYTE), 
	 CONSTRAINT "PK_NFERES" PRIMARY KEY ("CHAVE_ACESSO")
)
$

ALTER TABLE EMPRESA ADD DATA_ULTIMA_CONSULTA DATE
$

ALTER TABLE NFERES ADD ULTIMO_EVENTO VARCHAR2(100)
$


ALTER TABLE NFERES ADD DETALHE_EVENTO VARCHAR2(500)
$

ALTER TABLE LOGDAT ADD AUTO_CONSULTA_SEFAZ CHAR(1) DEFAULT 'N'
$